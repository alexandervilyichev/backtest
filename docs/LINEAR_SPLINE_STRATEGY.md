# Linear Spline Strategy V2

## Описание

Linear Spline Strategy V2 - это стратегия на основе линейных сплайнов, которая определяет точки разворота тренда постфактум, используя только исторические данные. В отличие от Predictive Spline Strategy, эта версия **не прогнозирует будущее**, а работает исключительно с фактическими данными.

## Принцип работы

Стратегия анализирует ценовые данные и строит чередующиеся линейные сплайны (восходящие и нисходящие тренды):

1. **Построение сплайнов**: Алгоритм подбирает оптимальные линейные сегменты, которые наилучшим образом аппроксимируют ценовые данные
2. **Чередование направлений**: Каждый следующий сплайн имеет противоположное направление (восходящий → нисходящий → восходящий)
3. **Определение разворотов**: Точки смены направления тренда определяются как точки разворота
4. **Генерация сигналов**: 
   - **BUY** - при переходе от нисходящего тренда к восходящему
   - **SELL** - при переходе от восходящего тренда к нисходящему

## Параметры стратегии

### MinSegmentLength (int)
- **Описание**: Минимальная длина сегмента тренда в свечах
- **Диапазон**: 2-50
- **По умолчанию**: 10
- **Влияние**: 
  - Меньшие значения → больше сигналов, выше чувствительность к краткосрочным движениям
  - Большие значения → меньше сигналов, фокус на долгосрочных трендах

### MaxSegmentLength (int)
- **Описание**: Максимальная длина сегмента тренда в свечах
- **Диапазон**: MinSegmentLength*2 - 200
- **По умолчанию**: 50
- **Влияние**: Ограничивает максимальную длину одного тренда

### MinR2Threshold (float64)
- **Описание**: Минимальный коэффициент детерминации (R²) для доверия к тренду
- **Диапазон**: 0.0 - 1.0
- **По умолчанию**: 0.6
- **Влияние**: 
  - Более высокие значения → только четкие тренды, меньше ложных сигналов
  - Более низкие значения → больше сигналов, но возможны ложные срабатывания

### MinSlopeThreshold (float64)
- **Описание**: Минимальный наклон линии для определения тренда
- **Диапазон**: 0.0001 - 0.01
- **По умолчанию**: 0.0005
- **Влияние**: Фильтрует слабые тренды с малым наклоном

## Математическая основа

### Линейная регрессия

Для каждого сегмента данных вычисляется линейная функция:

```
y = slope * x + intercept
```

Где:
- `slope` - наклон линии (скорость изменения цены)
- `intercept` - точка пересечения с осью Y

Коэффициенты вычисляются методом наименьших квадратов:

```
slope = (n*ΣXY - ΣX*ΣY) / (n*ΣX² - (ΣX)²)
intercept = (ΣY - slope*ΣX) / n
```

### Коэффициент детерминации (R²)

R² показывает, насколько хорошо линейная модель описывает данные:

```
R² = 1 - SSres/SStot
```

Где:
- `SSres` - сумма квадратов остатков (разница между фактическими и предсказанными значениями)
- `SStot` - общая сумма квадратов (разница между фактическими значениями и средним)

R² = 1.0 означает идеальное соответствие, R² = 0.0 - модель не лучше среднего значения.

## Алгоритм работы

```
1. Начать с индекса 0
2. Определить начальное направление тренда (по первым свечам)
3. Пока есть данные:
   a. Подобрать оптимальный линейный сегмент текущего направления
   b. Проверить качество аппроксимации (R² >= MinR2Threshold)
   c. Проверить наклон (|slope| >= MinSlopeThreshold)
   d. Если сегмент найден:
      - Добавить сегмент в список
      - Переместиться к концу сегмента
      - Сменить направление на противоположное
   e. Иначе:
      - Попробовать противоположное направление
      - Если не удалось - сдвинуться на 1 свечу
4. Сгенерировать сигналы в точках смены направления
```

## Преимущества

✅ **Простота**: Понятная логика без сложных математических моделей  
✅ **Надежность**: Работает только с фактическими данными, не прогнозирует  
✅ **Эффективность**: Низкая вычислительная сложность  
✅ **Трендовость**: Хорошо работает на трендовых рынках  
✅ **Гибкость**: Легко настраивается под разные таймфреймы  

## Недостатки

❌ **Запаздывание**: Определяет развороты с задержкой  
❌ **Боковые рынки**: Может давать ложные сигналы на флэте  
❌ **Шум**: Чувствительна к краткосрочным колебаниям  
❌ **Переоптимизация**: Требует осторожной настройки параметров  

## Лучшие условия применения

🎯 **Трендовые рынки** с четкими восходящими и нисходящими движениями  
🎯 **Средне- и долгосрочная торговля** (дневные, недельные графики)  
🎯 **Активы с низким шумом** и хорошей ликвидностью  
🎯 **Комбинация с фильтрами** (объем, волатильность)  

## Примеры использования

### Базовая конфигурация
```json
{
  "min_segment_length": 10,
  "max_segment_length": 50,
  "min_r2_threshold": 0.6,
  "min_slope_threshold": 0.0005
}
```

### Агрессивная (больше сигналов)
```json
{
  "min_segment_length": 5,
  "max_segment_length": 30,
  "min_r2_threshold": 0.5,
  "min_slope_threshold": 0.0001
}
```

### Консервативная (меньше сигналов)
```json
{
  "min_segment_length": 20,
  "max_segment_length": 100,
  "min_r2_threshold": 0.8,
  "min_slope_threshold": 0.001
}
```

## Результаты тестирования

### Тест на TMOS (1000 свечей)
- **Прибыль**: +19.29%
- **Сделок**: 12
- **Финальный капитал**: $11,929.11
- **Время выполнения**: 252ms

### Тест на TMOS Big (15,323 свечи)
- **Прибыль**: +1,187.14%
- **Сделок**: 215
- **Финальный капитал**: $128,714.45
- **Время выполнения**: 4.2s

## Сравнение с другими стратегиями

| Характеристика | Linear Spline V2 | Predictive Spline V2 | Linear Alternating V1 |
|----------------|------------------|----------------------|----------------------|
| Прогнозирование | ❌ Нет | ✅ Да | ❌ Нет |
| Сложность | Низкая | Высокая | Низкая |
| Запаздывание | Среднее | Низкое | Среднее |
| Архитектура | V2 | V2 | V1 |
| Оптимизация | Grid Search | Grid Search | Custom |

## Рекомендации по настройке

1. **Начните с дефолтных параметров** и оцените результаты
2. **Для краткосрочной торговли** уменьшите MinSegmentLength до 5-8
3. **Для долгосрочной торговли** увеличьте MinSegmentLength до 20-30
4. **На шумных рынках** повысьте MinR2Threshold до 0.7-0.8
5. **На трендовых рынках** можно снизить MinR2Threshold до 0.5-0.6
6. **Используйте оптимизацию** для подбора параметров под конкретный актив

## Код использования

```go
// Создание стратегии с дефолтными параметрами
strategy := NewLinearSplineStrategyV2(0.01) // slippage 1%

// Создание с кастомной конфигурацией
config := &LinearSplineConfig{
    MinSegmentLength:  10,
    MaxSegmentLength:  50,
    MinR2Threshold:    0.6,
    MinSlopeThreshold: 0.0005,
}

// Генерация сигналов
signals := strategy.GenerateSignals(candles, config)

// Запуск оптимизации
optimizedConfig := strategy.Optimize(candles)
```

## Запуск через CLI

```bash
# Тест на одном файле
./backtester -file tmos.json -strategy linear_spline_v2

# Тест с сохранением сигналов
./backtester -file tmos_big.json -strategy linear_spline_v2 -save_signals 5

# Тест с кастомной конфигурацией
./backtester -file tmos.json -strategy linear_spline_v2 -config my_config.json
```

## Дальнейшее развитие

Возможные улучшения стратегии:

1. **Адаптивные параметры**: Автоматическая подстройка под волатильность рынка
2. **Фильтры объема**: Учет объема торгов при определении разворотов
3. **Мультитаймфрейм**: Анализ нескольких таймфреймов одновременно
4. **Машинное обучение**: Использование ML для определения оптимальных параметров
5. **Динамические стопы**: Адаптивные стоп-лоссы на основе волатильности

## Заключение

Linear Spline Strategy V2 - это надежная и эффективная стратегия для трендовой торговли, которая не использует прогнозирование и работает исключительно с фактическими данными. Она хорошо подходит для средне- и долгосрочной торговли на трендовых рынках и может быть легко адаптирована под различные активы и таймфреймы.
