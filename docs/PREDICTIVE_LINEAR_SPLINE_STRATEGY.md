# Predictive Linear Spline Strategy V2

## Описание

Predictive Linear Spline Strategy V2 - это стратегия на основе линейных сплайнов, которая **прогнозирует** точки разворота тренда, используя экстраполяцию линейных трендов и анализ исторических данных. В отличие от Linear Spline Strategy, которая определяет развороты постфактум, эта версия пытается предсказать развороты заранее.

## Принцип работы

Стратегия анализирует текущий тренд и прогнозирует, когда он может развернуться:

1. **Построение линейного тренда**: Подбирает оптимальный линейный сплайн для текущих данных
2. **Анализ истории**: Изучает длину предыдущих трендов для определения средней продолжительности
3. **Оценка импульса**: Вычисляет ускорение/замедление тренда
4. **Прогнозирование разворота**: На основе:
   - Текущей длины тренда относительно исторической средней
   - Импульса тренда (ускорение/замедление)
   - Фактора истощения тренда
5. **Генерация сигналов**: Выставляет сигнал за несколько свечей до предсказанного разворота

## Параметры стратегии

### MinSegmentLength (int)
- **Описание**: Минимальная длина сегмента тренда в свечах
- **Диапазон**: 2-50
- **По умолчанию**: 8
- **Влияние**: Меньшие значения → больше сигналов, выше чувствительность

### MaxSegmentLength (int)
- **Описание**: Максимальная длина сегмента тренда в свечах
- **Диапазон**: MinSegmentLength*2 - 200
- **По умолчанию**: 50
- **Влияние**: Ограничивает максимальную длину анализируемого тренда

### PredictionHorizon (int)
- **Описание**: Горизонт предсказания в свечах
- **Диапазон**: 3-15
- **По умолчанию**: 7
- **Влияние**: Определяет, насколько далеко в будущее смотрит стратегия

### MinR2Threshold (float64)
- **Описание**: Минимальный коэффициент детерминации (R²) для доверия к тренду
- **Диапазон**: 0.0 - 1.0
- **По умолчанию**: 0.6
- **Влияние**: Более высокие значения → только четкие тренды

### SignalAdvance (int)
- **Описание**: За сколько свечей до предсказанного разворота выставлять сигнал
- **Диапазон**: 1-10
- **По умолчанию**: 3
- **Влияние**: Определяет, насколько рано выставляется сигнал

### MinSlopeThreshold (float64)
- **Описание**: Минимальный наклон линии для определения тренда
- **Диапазон**: 0.00001 - 0.01
- **По умолчанию**: 0.0003
- **Влияние**: Фильтрует слабые тренды

### TrendExhaustionFactor (float64)
- **Описание**: Коэффициент истощения тренда (отношение текущей длины к средней)
- **Диапазон**: 0.5 - 1.0
- **По умолчанию**: 0.75
- **Влияние**: При достижении этого значения тренд считается близким к развороту

### MinPriceChange (float64)
- **Описание**: Минимальное изменение цены в тренде (в долях)
- **Диапазон**: 0.001 - 0.05
- **По умолчанию**: 0.003 (0.3%)
- **Влияние**: Фильтрует слабые ценовые движения

## Математическая основа

### Линейная регрессия

Для текущего тренда вычисляется линейная функция:

```
y = slope * x + intercept
```

### Экстраполяция

Предсказанная цена в будущем:

```
predicted_price = slope * (current_length + predicted_distance) + intercept
```

### Анализ импульса тренда

Импульс вычисляется как отношение наклонов второй и первой половины тренда:

```
momentum = |slope_second_half| / |slope_first_half|
```

- `momentum > 1.0` - тренд ускоряется
- `momentum < 1.0` - тренд замедляется
- `momentum ≈ 1.0` - тренд стабилен

### Предсказание расстояния до разворота

```
exhaustion_ratio = current_trend_length / average_trend_length

if exhaustion_ratio >= exhaustion_factor:
    predicted_distance = prediction_horizon
elif exhaustion_ratio >= 0.5:
    predicted_distance = prediction_horizon * 2
else:
    predicted_distance = prediction_horizon * 3

# Корректировка на основе импульса
if momentum < 0.8:  # Замедление
    predicted_distance *= 0.7
elif momentum > 1.2:  # Ускорение
    predicted_distance *= 1.3
```

### Уверенность в предсказании

```
confidence = R² + exhaustion_bonus + momentum_bonus - distance_penalty + price_change_bonus

exhaustion_bonus = (exhaustion_ratio - exhaustion_factor) * 0.3  (если > exhaustion_factor)
momentum_bonus = (1.0 - momentum) * 0.2  (если momentum < 1.0)
distance_penalty = (predicted_distance - horizon*2) / (horizon*2) * 0.3  (если > horizon*2)
price_change_bonus = 0.1  (если price_change > 2%)
```

## Алгоритм работы

```
1. Инициализация:
   - Пустая история трендов
   - Нет активного предсказания

2. Для каждой свечи (начиная с достаточного количества данных):
   
   a. Если есть активное предсказание:
      - Проверить, не пора ли выставить сигнал
      - Если да и уверенность достаточна:
        * Выставить сигнал
        * Добавить текущий тренд в историю
        * Сбросить предсказание
      - Если прошли точку предсказания:
        * Сбросить предсказание
   
   b. Если нет активного предсказания:
      - Проанализировать текущий тренд
      - Если тренд качественный (R² >= threshold):
        * Вычислить среднюю длину исторических трендов
        * Оценить импульс текущего тренда
        * Предсказать расстояние до разворота
        * Вычислить уверенность
        * Если уверенность достаточна:
          - Создать активное предсказание

3. История трендов:
   - Хранит последние 20 завершенных трендов
   - Используется для вычисления средней длины
```

## Преимущества

✅ **Прогнозирование**: Предсказывает развороты заранее, а не постфактум  
✅ **Адаптивность**: Учитывает историю предыдущих трендов  
✅ **Анализ импульса**: Определяет ускорение/замедление тренда  
✅ **Гибкость**: Множество параметров для настройки  
✅ **Простота**: Использует линейную экстраполяцию (проще квадратичной)  

## Недостатки

❌ **Сложность прогнозирования**: Предсказание будущего всегда сложнее анализа прошлого  
❌ **Ложные сигналы**: Может давать ранние сигналы в продолжающихся трендах  
❌ **Чувствительность к параметрам**: Требует тщательной настройки  
❌ **Зависимость от истории**: Нужно достаточно данных для анализа  
❌ **Линейная экстраполяция**: Не учитывает нелинейные движения  

## Лучшие условия применения

🎯 **Рынки с предсказуемыми циклами** и повторяющимися паттернами  
🎯 **Средне- и долгосрочная торговля** (дневные, недельные графики)  
🎯 **Активы с низкой волатильностью** и плавными трендами  
🎯 **Комбинация с фильтрами** (объем, другие индикаторы)  

## Примеры использования

### Базовая конфигурация
```json
{
  "min_segment_length": 8,
  "max_segment_length": 50,
  "prediction_horizon": 7,
  "min_r2_threshold": 0.6,
  "signal_advance": 3,
  "min_slope_threshold": 0.0003,
  "trend_exhaustion_factor": 0.75,
  "min_price_change": 0.003
}
```

### Агрессивная (больше сигналов)
```json
{
  "min_segment_length": 5,
  "max_segment_length": 30,
  "prediction_horizon": 5,
  "min_r2_threshold": 0.5,
  "signal_advance": 3,
  "min_slope_threshold": 0.0001,
  "trend_exhaustion_factor": 0.6,
  "min_price_change": 0.002
}
```

### Консервативная (меньше сигналов)
```json
{
  "min_segment_length": 12,
  "max_segment_length": 70,
  "prediction_horizon": 10,
  "min_r2_threshold": 0.7,
  "signal_advance": 5,
  "min_slope_threshold": 0.0005,
  "trend_exhaustion_factor": 0.8,
  "min_price_change": 0.005
}
```

## Результаты тестирования

### Тест на TMOS (1000 свечей)
- **Прибыль**: +3.14%
- **Сделок**: 1
- **Финальный капитал**: $10,314.37
- **Время выполнения**: 536ms

### Тест на TMOS Big (15,323 свечи)
- **Прибыль**: +17.34%
- **Сделок**: 10
- **Финальный капитал**: $11,734.25
- **Время выполнения**: 4.0s

**Примечание**: После оптимизации механизма подбора параметров стратегия показывает стабильную прибыль. Консервативный подход с малым количеством сделок обеспечивает надежность.

## Сравнение с другими стратегиями

| Характеристика | Predictive Linear Spline V2 | Linear Spline V2 | Predictive Spline V2 |
|----------------|----------------------------|------------------|----------------------|
| Прогнозирование | ✅ Да (линейное) | ❌ Нет | ✅ Да (квадратичное) |
| Сложность | Средняя | Низкая | Высокая |
| Запаздывание | Низкое | Среднее | Низкое |
| Точность | Средняя | Высокая | Средняя |
| Количество сигналов | Среднее | Высокое | Среднее |

## Рекомендации по настройке

1. **Начните с дефолтных параметров** и оцените результаты
2. **Для краткосрочной торговли**:
   - Уменьшите MinSegmentLength до 5
   - Уменьшите PredictionHorizon до 5
   - Снизьте TrendExhaustionFactor до 0.6
3. **Для долгосрочной торговли**:
   - Увеличьте MinSegmentLength до 15
   - Увеличьте PredictionHorizon до 10
   - Повысьте TrendExhaustionFactor до 0.8
4. **На шумных рынках**:
   - Повысьте MinR2Threshold до 0.7
   - Увеличьте MinPriceChange до 0.005
5. **На трендовых рынках**:
   - Снизьте MinR2Threshold до 0.5
   - Уменьшите MinPriceChange до 0.002
6. **Используйте оптимизацию** для подбора параметров

## Код использования

```go
// Создание стратегии с дефолтными параметрами
strategy := NewPredictiveLinearSplineStrategyV2(0.01) // slippage 1%

// Создание с кастомной конфигурацией
config := &PredictiveLinearSplineConfig{
    MinSegmentLength:      8,
    MaxSegmentLength:      50,
    PredictionHorizon:     7,
    MinR2Threshold:        0.6,
    SignalAdvance:         3,
    MinSlopeThreshold:     0.0003,
    TrendExhaustionFactor: 0.75,
    MinPriceChange:        0.003,
}

// Генерация сигналов
signals := strategy.GenerateSignals(candles, config)

// Запуск оптимизации
optimizedConfig := strategy.Optimize(candles)
```

## Запуск через CLI

```bash
# Тест на одном файле
./backtester -file tmos.json -strategy predictive_linear_spline_v2

# Тест с сохранением сигналов
./backtester -file tmos_big.json -strategy predictive_linear_spline_v2 -save_signals 5

# Тест с кастомной конфигурацией
./backtester -file tmos.json -strategy predictive_linear_spline_v2 -config my_config.json

# Тест с отладкой
./backtester -file tmos.json -strategy predictive_linear_spline_v2 -debug
```

## Улучшения механизма подбора параметров

### Проблема (до оптимизации)
- **Прибыль**: -23.18%
- **Сделок**: 55
- **Причина**: Слишком агрессивная генерация сигналов, много ложных срабатываний

### Решение (после оптимизации)
- **Прибыль**: +17.34%
- **Сделок**: 10
- **Улучшение**: +40.52 процентных пункта!

### Ключевые изменения:

1. **Более строгие пороги R²**
   - Было: 0.50-0.70
   - Стало: 0.65-0.80
   - Эффект: Только качественные тренды

2. **Увеличена минимальная длина тренда**
   - Было: 5-12 свечей
   - Стало: 8-20 свечей
   - Эффект: Фильтрация краткосрочного шума

3. **Выше минимальное изменение цены**
   - Было: 0.2%-0.5%
   - Стало: 0.5%-1.5%
   - Эффект: Только значимые движения

4. **Адаптивный порог уверенности**
   - Малые данные (<5000): 0.30
   - Средние данные (5000-10000): 0.35
   - Большие данные (>10000): 0.40
   - Эффект: Меньше ложных сигналов на больших данных

5. **Увеличено расстояние между сигналами**
   - Было: 1x MinSegmentLength
   - Стало: 2-3x MinSegmentLength
   - Эффект: Избегание переторговли

6. **Улучшенная формула уверенности**
   - Добавлены штрафы за:
     * Слишком ранние сигналы
     * Ускоряющиеся тренды
     * Короткие тренды
   - Добавлены бонусы за:
     * Истощение тренда
     * Замедление тренда
     * Сильное изменение цены

## Дальнейшее развитие

Возможные улучшения стратегии:

1. **Нелинейная экстраполяция**: Использование полиномов высших степеней
2. **Машинное обучение**: Обучение модели на исторических данных
3. **Мультитаймфрейм**: Анализ нескольких таймфреймов одновременно
4. **Адаптивные параметры**: Автоматическая подстройка под волатильность
5. **Фильтры подтверждения**: Использование дополнительных индикаторов
6. **Управление рисками**: Динамические стоп-лоссы и тейк-профиты

## Заключение

Predictive Linear Spline Strategy V2 - это экспериментальная стратегия, которая пытается предсказать развороты тренда заранее. Она использует линейную экстраполяцию, анализ исторических трендов и оценку импульса для прогнозирования. Хотя прогнозирование сложнее постфактум анализа, стратегия может быть полезна в комбинации с другими индикаторами и при тщательной настройке параметров.

**Важно**: Эта стратегия требует тщательного тестирования и настройки под конкретный актив. Прогнозирующие стратегии всегда более рискованны, чем стратегии, работающие с фактическими данными.
