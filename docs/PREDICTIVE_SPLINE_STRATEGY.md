# Predictive Quadratic Spline Strategy (v2)

## 📋 Описание

Стратегия **Predictive Quadratic Spline** - это улучшенная версия стратегии `quadratic_variable_trend_spline` (v1), которая не просто определяет точки разворота тренда постфактум, а **предсказывает их заранее** и выставляет торговые сигналы в будущее.

## 🔄 Отличия от v1 версии

### V1 (quadratic_variable_trend_spline)
- ✅ Отлично определяет точки разворота на исторических данных
- ❌ Выставляет сигналы **постфактум** (когда разворот уже произошел)
- ❌ Не может использоваться для реальной торговли
- ✅ Полезна для анализа и понимания трендов

### V2 (predictive_spline)
- ✅ **Предсказывает** будущие точки разворота
- ✅ Выставляет сигналы **заранее** (за несколько свечей до разворота)
- ✅ Может использоваться для реальной торговли
- ✅ Использует экстраполяцию квадратичных функций
- ✅ Учитывает уверенность в предсказании (R²)

## 🧮 Математическая основа

### Квадратичная регрессия

Стратегия моделирует ценовой тренд квадратичной функцией:

```
P(x) = a·x² + b·x + c
```

где:
- `a` - коэффициент кривизны (определяет форму параболы)
- `b` - коэффициент наклона
- `c` - начальная цена
- `x` - индекс свечи в сегменте

### Точка перегиба

Точка перегиба (где производная = 0) вычисляется как:

```
x_inflection = -b / (2a)
```

Это точка, где тренд меняет направление с ускорения на замедление (или наоборот).

### Экстраполяция

Для предсказания будущей цены используется та же квадратичная функция:

```
P_future = a·x_future² + b·x_future + c
```

где `x_future` - индекс будущей свечи.

### Коэффициент детерминации (R²)

R² показывает, насколько хорошо модель описывает данные:

```
R² = 1 - (SS_res / SS_tot)
```

где:
- `SS_res` - сумма квадратов остатков
- `SS_tot` - общая сумма квадратов

R² ∈ [0, 1], где:
- R² = 1 - идеальное соответствие
- R² = 0 - модель не лучше среднего значения

## 🎯 Алгоритм работы

### 1. Анализ текущего тренда

На каждой свече стратегия:

1. Берет окно данных размером от `MinSegmentLength` до `MaxSegmentLength`
2. Строит квадратичную регрессию для разных длин окна
3. Выбирает модель с наилучшим R²
4. Проверяет, что R² ≥ `MinR2Threshold`

### 2. Предсказание точки разворота

Стратегия определяет ожидаемую точку разворота на основе:

**A. Точки перегиба квадратичной функции**
```
Если точка перегиба в пределах горизонта предсказания:
    predicted_distance = inflection_x - current_x
```

**B. Исторической длины трендов**
```
Иначе:
    avg_segment_length = (min_length + max_length) / 2
    
    Если текущий сегмент >= avg_segment_length:
        predicted_distance = prediction_horizon
    Иначе:
        predicted_distance = avg_segment_length - current_segment_length
```

### 3. Генерация сигнала

Сигнал выставляется за `SignalAdvance` свечей до предсказанной точки разворота:

```
signal_index = predicted_reversal_index - signal_advance
```

Тип сигнала противоположен текущему тренду:
- Восходящий тренд → SELL (ожидаем разворот вниз)
- Нисходящий тренд → BUY (ожидаем разворот вверх)

### 4. Уверенность в предсказании

```
confidence = R² × (1 - distance_to_prediction / max_horizon)
```

Сигнал выставляется только если `confidence > 0.3`

## ⚙️ Параметры стратегии

| Параметр | Описание | По умолчанию | Диапазон оптимизации |
|----------|----------|--------------|---------------------|
| `MinSegmentLength` | Минимальная длина сегмента для анализа | 5 | 5-15 |
| `MaxSegmentLength` | Максимальная длина сегмента для анализа | 50 | 30-80 |
| `PredictionHorizon` | Горизонт предсказания (в свечах) | 5 | 3-10 |
| `MinR2Threshold` | Минимальный R² для доверия к модели | 0.7 | 0.6-0.9 |
| `SignalAdvance` | За сколько свечей до разворота выставлять сигнал | 3 | 2-6 |

### Влияние параметров

**MinSegmentLength / MaxSegmentLength**
- Меньше → более чувствительна к краткосрочным трендам
- Больше → более плавные, долгосрочные тренды

**PredictionHorizon**
- Меньше → более консервативные предсказания
- Больше → более агрессивные предсказания (но менее точные)

**MinR2Threshold**
- Выше → меньше сигналов, но более надежных
- Ниже → больше сигналов, но менее надежных

**SignalAdvance**
- Меньше → сигналы ближе к развороту (меньше риск)
- Больше → сигналы раньше (больше потенциальная прибыль, но выше риск)

## 📊 Пример использования

### Запуск с параметрами по умолчанию

```bash
./backtester -file tmos.json -strategy predictive_spline_v2
```

### Результаты тестирования

```
════════════════════════════════════════════════════════════════════════════════
🎯 ТЕСТИРОВАНИЕ ОДИНОЧНОЙ СТРАТЕГИИ
════════════════════════════════════════════════════════════════════════════════
📈 Стратегия: predictive_spline_v2
📊 Данных для анализа: 1000 свечей
────────────────────────────────────────────────────────────────────────────────
🔄 Оптимизация параметров...
Best config found: PredictiveSpline(min_len=5, max_len=50, horizon=9, r2=0.70, advance=2)
💹 Прибыль: +3.70%
📊 Сделок: 38
────────────────────────────────────────────────────────────────────────────────
```

## 💪 Сильные стороны

1. **Предсказательная способность** - выставляет сигналы заранее, а не постфактум
2. **Математическая обоснованность** - использует квадратичную регрессию
3. **Контроль качества** - учитывает R² для оценки надежности модели
4. **Адаптивность** - подстраивается под разные длины трендов
5. **Гибкость** - множество параметров для настройки под разные рынки

## ⚠️ Слабые стороны

1. **Экстраполяция неточна** - будущее может отличаться от модели
2. **Чувствительность к шуму** - выбросы могут исказить модель
3. **Ранние сигналы** - может выставлять сигналы слишком рано в сильных трендах
4. **Требует данных** - нужно достаточно свечей для построения модели
5. **Квадратичная модель** - не всегда подходит для всех типов трендов

## 🎯 Лучшие условия применения

✅ **Подходит для:**
- Рынки с плавными, предсказуемыми трендами
- Средне- и долгосрочная торговля (30 мин - 1 день)
- Активы с хорошей ликвидностью
- Комбинация с индикаторами подтверждения

❌ **Не подходит для:**
- Высоковолатильные рынки с резкими движениями
- Скальпинг и краткосрочная торговля
- Боковые рынки без четких трендов
- Рынки с частыми гэпами

## 🔧 Рекомендации по оптимизации

### Для разных таймфреймов

**Краткосрочные (1-5 мин)**
```
MinSegmentLength: 3-5
MaxSegmentLength: 20-30
PredictionHorizon: 3-5
SignalAdvance: 1-2
```

**Среднесрочные (15-30 мин)**
```
MinSegmentLength: 5-10
MaxSegmentLength: 40-60
PredictionHorizon: 5-7
SignalAdvance: 2-4
```

**Долгосрочные (1 час - 1 день)**
```
MinSegmentLength: 10-15
MaxSegmentLength: 60-100
PredictionHorizon: 7-10
SignalAdvance: 4-6
```

### Для разных типов рынков

**Трендовый рынок**
- Увеличить `MaxSegmentLength` для захвата длинных трендов
- Увеличить `PredictionHorizon` для более ранних сигналов
- Снизить `MinR2Threshold` для большего количества сигналов

**Волатильный рынок**
- Уменьшить `MaxSegmentLength` для быстрой адаптации
- Уменьшить `PredictionHorizon` для более консервативных предсказаний
- Повысить `MinR2Threshold` для фильтрации шума

## 📈 Сравнение с другими стратегиями

| Стратегия | Тип | Предсказание | Сложность | Прибыль (тест) |
|-----------|-----|--------------|-----------|----------------|
| predictive_spline_v2 | Тренд | ✅ Да | Средняя | +3.70% |
| quadratic_variable_trend_spline (v1) | Тренд | ❌ Нет | Средняя | N/A (постфактум) |
| elliott_wave_v2 | Волны | ✅ Да | Высокая | Варьируется |
| golden_cross_v2 | Тренд | ❌ Нет | Низкая | Варьируется |

## 🔬 Технические детали

### Структура кода

```go
// Конфигурация
type PredictiveSplineConfig struct {
    MinSegmentLength  int
    MaxSegmentLength  int
    PredictionHorizon int
    MinR2Threshold    float64
    SignalAdvance     int
}

// Сегмент сплайна
type SplineSegment struct {
    StartIdx    int
    EndIdx      int
    A, B, C     float64  // Коэффициенты квадратичной функции
    R2          float64  // Коэффициент детерминации
    IsAscending bool     // Направление тренда
    InflectionX float64  // Точка перегиба
}

// Предсказанный разворот
type PredictedReversal struct {
    PredictedIndex int
    PredictedPrice float64
    Confidence     float64
    SignalType     internal.SignalType
}
```

### Архитектура V2

Стратегия использует современную архитектуру V2 с композицией компонентов:

1. **SignalGenerator** - генерирует торговые сигналы
2. **ConfigManager** - управляет конфигурацией
3. **ConfigGenerator** - генерирует варианты для оптимизации
4. **GridSearchOptimizer** - оптимизирует параметры
5. **SlippageProvider** - учитывает проскальзывание

## 📚 Дополнительные материалы

- [V2 Strategy Guide](V2_STRATEGY_GUIDE.md) - руководство по архитектуре V2
- [Architecture Comparison](ARCHITECTURE_COMPARISON.md) - сравнение V1 и V2
- [Refactoring Guide](REFACTORING_GUIDE.md) - миграция с V1 на V2

## 🚀 Дальнейшее развитие

Возможные улучшения:

1. **Кубические сплайны** - для более точного моделирования сложных трендов
2. **Адаптивный горизонт** - динамическое изменение горизонта предсказания
3. **Множественные модели** - ансамбль из нескольких квадратичных моделей
4. **Машинное обучение** - обучение на исторических данных для улучшения предсказаний
5. **Фильтры шума** - предварительная обработка данных для снижения влияния выбросов
