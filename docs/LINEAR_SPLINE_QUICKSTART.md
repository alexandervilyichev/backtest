# Linear Spline Strategy V2 - Быстрый старт

## Что это?

Linear Spline Strategy V2 - это простая и эффективная стратегия для трендовой торговли, которая:
- ✅ Определяет развороты тренда постфактум
- ✅ Не прогнозирует будущее (работает только с фактами)
- ✅ Использует линейные сплайны для моделирования трендов
- ✅ Показывает отличные результаты на трендовых рынках

## Быстрый запуск

### 1. Базовый тест

```bash
# Тест на небольшом наборе данных (1000 свечей)
./backtester -file tmos.json -strategy linear_spline_v2
```

**Ожидаемый результат:**
```
✅ Загружено 1000 свечей из tmos.json
🔄 Оптимизация параметров...
Best config found: LinearSpline(min_len=5, max_len=30, r2=0.80, slope=0.0001) with profit: 0.1929

│ 🥇 1  │ linear_spline_v2          │ 🟢 +19.29%    │ 12       │ $11929.11       │ 252ms      │ Отлично  │
```

### 2. Тест на большом наборе данных

```bash
# Тест на полном наборе данных (15000+ свечей)
./backtester -file tmos_big.json -strategy linear_spline_v2
```

**Ожидаемый результат:**
```
✅ Загружено 15323 свечей из tmos_big.json
🔄 Оптимизация параметров...
Best config found: LinearSpline(min_len=5, max_len=30, r2=0.80, slope=0.0001) with profit: 11.8714

│ 🥇 1  │ linear_spline_v2          │ 🟢 +1187.14%  │ 215      │ $128714.45      │ 4.2s       │ Отлично  │
```

### 3. Сохранение сигналов для визуализации

```bash
# Тест с сохранением сигналов для графика
./backtester -file tmos_big.json -strategy linear_spline_v2 -save_signals=1
```

Это создаст файл `tmos_big_linear_spline_v2_signals.json` для визуализации в `chart_visualization.html`.

## Настройка параметров

### Создание конфигурационного файла

Создайте файл `linear_spline_config.json`:

```json
{
  "linear_spline_v2": {
    "min_segment_length": 10,
    "max_segment_length": 50,
    "min_r2_threshold": 0.6,
    "min_slope_threshold": 0.0005
  }
}
```

### Запуск с кастомной конфигурацией

```bash
./backtester -file tmos_big.json -strategy linear_spline_v2 -config linear_spline_config.json
```

## Примеры конфигураций

### Агрессивная (больше сигналов)

```json
{
  "linear_spline_v2": {
    "min_segment_length": 5,
    "max_segment_length": 30,
    "min_r2_threshold": 0.5,
    "min_slope_threshold": 0.0001
  }
}
```

**Характеристики:**
- Больше сделок (200-300)
- Выше чувствительность к краткосрочным движениям
- Может давать ложные сигналы на боковых рынках

### Консервативная (меньше сигналов)

```json
{
  "linear_spline_v2": {
    "min_segment_length": 20,
    "max_segment_length": 100,
    "min_r2_threshold": 0.8,
    "min_slope_threshold": 0.001
  }
}
```

**Характеристики:**
- Меньше сделок (50-100)
- Только четкие тренды
- Меньше ложных сигналов

### Сбалансированная (рекомендуется)

```json
{
  "linear_spline_v2": {
    "min_segment_length": 10,
    "max_segment_length": 50,
    "min_r2_threshold": 0.6,
    "min_slope_threshold": 0.0005
  }
}
```

**Характеристики:**
- Оптимальное количество сделок (100-200)
- Баланс между чувствительностью и надежностью
- Хорошо работает на большинстве рынков

## Интерпретация результатов

### Метрики производительности

```
│ Ранг │ Стратегия         │ Прибыль      │ Сделки   │ Финал, $    │ Время  │ Статус   │
├──────┼───────────────────┼──────────────┼──────────┼─────────────┼────────┼──────────┤
│ 🥇 1  │ linear_spline_v2  │ 🟢 +1187.14% │ 215      │ $128714.45  │ 4.2s   │ Отлично  │
```

**Что означают метрики:**

- **Прибыль**: Общая доходность стратегии в процентах
  - 🟢 Зеленый (>10%) - отличный результат
  - 🟠 Оранжевый (0-10%) - средний результат
  - 🔴 Красный (<0%) - убыточная стратегия

- **Сделки**: Количество совершенных сделок
  - Слишком мало (<10) - возможно, упущены возможности
  - Оптимально (50-200) - хороший баланс
  - Слишком много (>500) - возможно, переторговля

- **Финал, $**: Итоговый капитал (начальный = $10,000)

- **Время**: Время выполнения оптимизации и бэктестинга

- **Статус**: Общая оценка производительности

### Оптимальные параметры

```
Best config found: LinearSpline(min_len=5, max_len=30, r2=0.80, slope=0.0001) with profit: 11.8714
```

**Расшифровка:**
- `min_len=5` - минимальная длина тренда 5 свечей
- `max_len=30` - максимальная длина тренда 30 свечей
- `r2=0.80` - высокое качество аппроксимации (80%)
- `slope=0.0001` - минимальный наклон для определения тренда
- `profit: 11.8714` - прибыль в абсолютных единицах (1187.14%)

## Сравнение с другими стратегиями

### Запуск всех стратегий

```bash
./backtester -file tmos_big.json -strategy all
```

Это позволит сравнить Linear Spline V2 с другими стратегиями:

```
│ Ранг │ Стратегия              │ Прибыль      │ Сделки   │ Финал, $    │
├──────┼────────────────────────┼──────────────┼──────────┼─────────────┤
│ 🥇 1  │ linear_spline_v2       │ 🟢 +1187.14% │ 215      │ $128714.45  │
│ 🥈 2  │ predictive_spline_v2   │ 🟢 +856.32%  │ 189      │ $95632.10   │
│ 🥉 3  │ cci_oscillator         │ 🟢 +234.56%  │ 156      │ $33456.78   │
│   4  │ rsi_oscillator         │ 🟢 +123.45%  │ 142      │ $22345.67   │
│   5  │ buy_and_hold           │ 🟠 -1.26%    │ 0        │ $9873.82    │
```

## Визуализация результатов

### 1. Сохраните сигналы

```bash
./backtester -file tmos_big.json -strategy linear_spline_v2 -save_signals=1
```

### 2. Откройте визуализатор

```bash
# Запустите локальный сервер
python -m http.server 8000

# Откройте в браузере
http://localhost:8000/chart_visualization.html?file=tmos_big_linear_spline_v2_signals.json
```

### 3. Анализируйте график

- **Зеленые маркеры** - сигналы BUY (покупка)
- **Красные маркеры** - сигналы SELL (продажа)
- **Наведите курсор** на свечу для детальной информации
- **Масштабируйте** колесиком мыши для детального анализа

## Типичные проблемы и решения

### Проблема: Слишком мало сделок

**Причина:** Слишком строгие параметры фильтрации

**Решение:**
```json
{
  "min_segment_length": 5,      // Уменьшите
  "max_segment_length": 30,     // Уменьшите
  "min_r2_threshold": 0.5,      // Снизьте
  "min_slope_threshold": 0.0001 // Снизьте
}
```

### Проблема: Слишком много сделок

**Причина:** Слишком мягкие параметры, стратегия реагирует на шум

**Решение:**
```json
{
  "min_segment_length": 15,     // Увеличьте
  "max_segment_length": 80,     // Увеличьте
  "min_r2_threshold": 0.75,     // Повысьте
  "min_slope_threshold": 0.001  // Повысьте
}
```

### Проблема: Низкая прибыль

**Причина:** Параметры не оптимизированы под конкретный актив

**Решение:**
1. Запустите без конфигурации для автоматической оптимизации
2. Используйте найденные оптимальные параметры
3. Протестируйте на разных периодах

## Следующие шаги

1. **Изучите документацию**: [LINEAR_SPLINE_STRATEGY.md](LINEAR_SPLINE_STRATEGY.md)
2. **Сравните со стратегиями**: [SPLINE_STRATEGY_COMPARISON.md](SPLINE_STRATEGY_COMPARISON.md)
3. **Попробуйте Predictive Spline**: [PREDICTIVE_SPLINE_QUICKSTART.md](PREDICTIVE_SPLINE_QUICKSTART.md)
4. **Экспериментируйте** с параметрами на своих данных

## Полезные команды

```bash
# Базовый тест
./backtester -file tmos.json -strategy linear_spline_v2

# Тест с детальным логированием
./backtester -file tmos_big.json -strategy linear_spline_v2 -debug

# Тест с сохранением сигналов
./backtester -file tmos_big.json -strategy linear_spline_v2 -save_signals=1

# Тест с кастомной конфигурацией
./backtester -file tmos_big.json -strategy linear_spline_v2 -config my_config.json

# Сравнение всех стратегий
./backtester -file tmos_big.json -strategy all

# Профилирование производительности
./backtester -file tmos_big.json -strategy linear_spline_v2 -cpu_profile cpu.prof -mem_profile mem.prof
```

## Заключение

Linear Spline Strategy V2 - это надежная и простая стратегия для трендовой торговли. Она не требует сложной настройки и показывает отличные результаты на трендовых рынках. Начните с дефолтных параметров и экспериментируйте для достижения лучших результатов на ваших данных!
