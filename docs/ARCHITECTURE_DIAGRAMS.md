# 📐 Архитектурные диаграммы

## 🔴 Старая архитектура (Встраивание)

```
┌─────────────────────────────────────────────────────────┐
│                    BaseStrategy                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │              BaseConfig                           │  │
│  │  - Config: StrategyConfig                         │  │
│  │  - slippage: float64                              │  │
│  │                                                    │  │
│  │  + DefaultConfig()                                │  │
│  │  + GetSlippage()                                  │  │
│  │  + SetSlippage()                                  │  │
│  │  + LoadConfigFromMap()                            │  │
│  └───────────────────────────────────────────────────┘  │
│                                                          │
│  + ProcessConfigs()  ← Дублируется в каждой стратегии  │
└─────────────────────────────────────────────────────────┘
                          ▲
                          │ Встраивание (жесткая связь)
                          │
┌─────────────────────────────────────────────────────────┐
│              GoldenCrossStrategy                        │
│  ┌───────────────────────────────────────────────────┐  │
│  │              BaseConfig                           │  │ ← Встраивание
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │              BaseStrategy                         │  │ ← Встраивание
│  └───────────────────────────────────────────────────┘  │
│                                                          │
│  + Name()                                               │
│  + GenerateSignalsWithConfig()  ← Уникальная логика    │
│  + OptimizeWithConfig()         ← Дублирование!        │
└─────────────────────────────────────────────────────────┘

ПРОБЛЕМЫ:
❌ Жесткая связь через встраивание
❌ Дублирование OptimizeWithConfig в каждой стратегии
❌ Невозможно заменить компоненты
❌ Сложно тестировать
❌ Нарушение SRP (одна структура делает всё)
```

---

## 🟢 Новая архитектура (Композиция + Интерфейсы)

```
┌──────────────────────────────────────────────────────────────────┐
│                         ИНТЕРФЕЙСЫ                               │
│  (определяют контракты, а не реализацию)                         │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────┐  ┌────────────────────┐                 │
│  │ SignalGenerator    │  │ ConfigOptimizer    │                 │
│  ├────────────────────┤  ├────────────────────┤                 │
│  │ + GenerateSignals()│  │ + Optimize()       │                 │
│  └────────────────────┘  └────────────────────┘                 │
│                                                                  │
│  ┌────────────────────┐  ┌────────────────────┐                 │
│  │ ConfigManager      │  │ TradingStrategy    │                 │
│  ├────────────────────┤  ├────────────────────┤                 │
│  │ + DefaultConfig()  │  │ + Name()           │                 │
│  │ + LoadFromJSON()   │  │ + GenerateSignals()│                 │
│  └────────────────────┘  │ + Optimize()       │                 │
│                          └────────────────────┘                 │
└──────────────────────────────────────────────────────────────────┘
                                  ▲
                                  │ Реализуют интерфейсы
                                  │
┌──────────────────────────────────────────────────────────────────┐
│                      РЕАЛИЗАЦИИ                                  │
│  (конкретные компоненты)                                         │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────┐  ┌──────────────────────────┐     │
│  │ GoldenCrossSignalGen     │  │ GridSearchOptimizer      │     │
│  ├──────────────────────────┤  ├──────────────────────────┤     │
│  │ + GenerateSignals()      │  │ + Optimize()             │     │
│  │   - Логика Golden Cross  │  │   - Grid Search          │     │
│  └──────────────────────────┘  └──────────────────────────┘     │
│                                          ▲                       │
│  ┌──────────────────────────┐            │                       │
│  │ SMASignalGenerator       │            │ Переиспользуется!    │
│  ├──────────────────────────┤            │                       │
│  │ + GenerateSignals()      │  ┌──────────────────────────┐     │
│  │   - Логика SMA           │  │ GeneticOptimizer         │     │
│  └──────────────────────────┘  ├──────────────────────────┤     │
│                                │ + Optimize()             │     │
│  ┌──────────────────────────┐  │   - Генетический алгоритм│     │
│  │ RSISignalGenerator       │  └──────────────────────────┘     │
│  ├──────────────────────────┤                                   │
│  │ + GenerateSignals()      │                                   │
│  │   - Логика RSI           │                                   │
│  └──────────────────────────┘                                   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                                  │
                                  │ Композиция
                                  ▼
┌──────────────────────────────────────────────────────────────────┐
│                      StrategyBase                                │
│  (собирает компоненты через композицию)                          │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  - name: string                                                  │
│  - signalGenerator: SignalGenerator      ← Интерфейс!           │
│  - configManager: ConfigManager          ← Интерфейс!           │
│  - configOptimizer: ConfigOptimizer      ← Интерфейс!           │
│  - slippageProvider: *SlippageProvider                           │
│                                                                  │
│  + Name()                                                        │
│  + GenerateSignals()  → делегирует signalGenerator              │
│  + Optimize()         → делегирует configOptimizer              │
│  + DefaultConfig()    → делегирует configManager                │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘

ПРЕИМУЩЕСТВА:
✅ Слабая связанность через интерфейсы
✅ Нет дублирования (GridSearchOptimizer переиспользуется)
✅ Легко заменить любой компонент
✅ Каждый компонент тестируется независимо
✅ Соблюдение SOLID принципов
```

---

## 🔄 Поток создания стратегии

### Старая архитектура:

```
NewGoldenCrossStrategy()
    │
    ├─> Создает BaseConfig (встраивание)
    │       │
    │       └─> Жестко связан с конкретной реализацией
    │
    └─> Создает BaseStrategy (встраивание)
            │
            └─> Дублирует OptimizeWithConfig

Результат: Жесткая связь, дублирование кода
```

### Новая архитектура:

```
NewGoldenCrossStrategy(slippage)
    │
    ├─> NewSlippageProvider(slippage)
    │       │
    │       └─> Независимый компонент
    │
    ├─> NewGoldenCrossSignalGenerator()
    │       │
    │       └─> Реализует SignalGenerator
    │
    ├─> NewConfigManager(defaultConfig, factory)
    │       │
    │       └─> Реализует ConfigManager
    │
    ├─> NewGoldenCrossConfigGenerator(params)
    │       │
    │       └─> Генерирует конфигурации для оптимизации
    │
    ├─> NewGridSearchOptimizer(slippageProvider, configGenerator)
    │       │
    │       └─> Реализует ConfigOptimizer (переиспользуется!)
    │
    └─> NewStrategyBase(name, generator, manager, optimizer, provider)
            │
            └─> Собирает всё через композицию

Результат: Гибкость, переиспользование, тестируемость
```

---

## 🧩 Диаграмма зависимостей

### Старая архитектура:

```
GoldenCrossStrategy
    │
    ├──> BaseStrategy (жесткая зависимость)
    │       │
    │       └──> BaseConfig (жесткая зависимость)
    │
    └──> Дублирует OptimizeWithConfig

RSIStrategy
    │
    ├──> BaseStrategy (жесткая зависимость)
    │       │
    │       └──> BaseConfig (жесткая зависимость)
    │
    └──> Дублирует OptimizeWithConfig

Проблема: Изменение BaseStrategy затрагивает ВСЕ стратегии
```

### Новая архитектура:

```
                    ┌─────────────────┐
                    │  Интерфейсы     │
                    │  (контракты)    │
                    └────────┬────────┘
                             │
                ┌────────────┼────────────┐
                │            │            │
        ┌───────▼──────┐ ┌──▼──────┐ ┌──▼──────────┐
        │SignalGenerator│ │Optimizer│ │ConfigManager│
        └───────┬──────┘ └──┬──────┘ └──┬──────────┘
                │            │            │
    ┌───────────┼────────────┼────────────┼───────────┐
    │           │            │            │           │
┌───▼───┐  ┌───▼───┐   ┌────▼────┐  ┌───▼───┐  ┌───▼───┐
│Golden │  │  SMA  │   │  Grid   │  │Genetic│  │Config │
│Cross  │  │Signal │   │ Search  │  │Optim. │  │Manager│
│Signal │  │  Gen  │   │Optimizer│  │       │  │ Impl  │
└───────┘  └───────┘   └────┬────┘  └───────┘  └───────┘
                             │
                    Переиспользуется
                    всеми стратегиями!

Преимущество: Изменение одного компонента не затрагивает другие
```

---

## 🎯 Диаграмма взаимодействия

### Генерация сигналов:

```
Пользователь
    │
    │ strategy.GenerateSignals(candles, config)
    ▼
StrategyBase
    │
    │ Делегирует
    ▼
SignalGenerator (интерфейс)
    │
    │ Реализация
    ▼
GoldenCrossSignalGenerator
    │
    │ 1. Извлекает цены
    │ 2. Вычисляет EMA
    │ 3. Определяет пересечения
    │ 4. Генерирует сигналы
    ▼
[]SignalType
```

### Оптимизация:

```
Пользователь
    │
    │ strategy.Optimize(candles, generator)
    ▼
StrategyBase
    │
    │ Делегирует
    ▼
ConfigOptimizer (интерфейс)
    │
    │ Реализация
    ▼
GridSearchOptimizer
    │
    ├─> ConfigGenerator.Generate()
    │       │
    │       └─> []StrategyConfig
    │
    ├─> Для каждой конфигурации:
    │   │
    │   ├─> SignalGenerator.GenerateSignals()
    │   │       │
    │   │       └─> []SignalType
    │   │
    │   └─> Backtest(candles, signals, slippage)
    │           │
    │           └─> BacktestResult
    │
    └─> Выбирает лучшую конфигурацию
        │
        └─> StrategyConfig
```

---

## 📊 Сравнение сложности

### Добавление новой стратегии:

**Старая архитектура:**
```
1. Создать структуру с встраиванием BaseStrategy
2. Реализовать GenerateSignalsWithConfig()
3. Дублировать OptimizeWithConfig() (~50 строк)
4. Реализовать конфигурацию
5. Зарегистрировать стратегию

Итого: ~150 строк кода, 50 из них дублирование
```

**Новая архитектура:**
```
1. Реализовать SignalGenerator (~30 строк)
2. Реализовать ConfigGenerator (~20 строк)
3. Создать фабричную функцию (~30 строк)
4. Реализовать конфигурацию (~20 строк)

Итого: ~100 строк кода, 0 дублирования
Оптимизатор переиспользуется!
```

### Изменение алгоритма оптимизации:

**Старая архитектура:**
```
1. Изменить BaseStrategy.ProcessConfigs()
2. Затрагивает ВСЕ стратегии
3. Нужно тестировать ВСЕ стратегии
4. Риск сломать существующий код

Сложность: ВЫСОКАЯ
```

**Новая архитектура:**
```
1. Создать новый оптимизатор (реализует ConfigOptimizer)
2. Не затрагивает существующий код
3. Тестируем только новый оптимизатор
4. Старый код продолжает работать

Сложность: НИЗКАЯ
```

---

## 🎓 Ключевые принципы

### 1. Зависимость от абстракций (DIP)

```
❌ Плохо:
type Strategy struct {
    optimizer *GridSearchOptimizer  // конкретный тип
}

✅ Хорошо:
type Strategy struct {
    optimizer ConfigOptimizer  // интерфейс
}
```

### 2. Композиция вместо наследования

```
❌ Плохо (встраивание):
type Strategy struct {
    BaseStrategy  // "is-a" отношение
}

✅ Хорошо (композиция):
type Strategy struct {
    generator SignalGenerator  // "has-a" отношение
}
```

### 3. Маленькие интерфейсы (ISP)

```
❌ Плохо (большой интерфейс):
type Strategy interface {
    GenerateSignals()
    Optimize()
    Backtest()
    SaveResults()
    LoadConfig()
}

✅ Хорошо (маленькие интерфейсы):
type SignalGenerator interface {
    GenerateSignals()
}

type ConfigOptimizer interface {
    Optimize()
}
```

---

## 🚀 Результат

**Старая архитектура:**
- 🔴 Жесткая связанность
- 🔴 Дублирование кода
- 🔴 Сложно тестировать
- 🔴 Сложно расширять

**Новая архитектура:**
- 🟢 Слабая связанность
- 🟢 Нет дублирования
- 🟢 Легко тестировать
- 🟢 Легко расширять
- 🟢 Соблюдение SOLID
- 🟢 Высокая переиспользуемость
